# Контракт проекта — единый источник правды

- **Проект:** `resume` — страница-резюме Алексея Коняева
- **Версия контракта:** `0.6`
- **Статус:** `DRAFT`
- **Последняя правка:** `2026-08-29`

---

## 1. PRD — требования

### 1.1 Цель и пользователи

Ссылка, которую владелец отправляет вместо файла резюме: рекрутеру, нанимающему
руководителю, заказчику автоматизации. Задача страницы — за один экран показать,
какие процессы человек перестроил и с каким измеримым результатом, и дать способ
связаться.

Позиционирование одно: **специалист по LegalOPS и AI-разработке**. Страница
должна выглядеть как рабочая среда инженера, а не как документ, — оформление
само по себе аргумент в пользу технической части профиля.

### 1.2 Фичи по приоритету

1. Содержание резюме из одного файла данных, интерфейс собирается из него.
2. Лента результатов с числовыми метриками.
3. Опыт работы: место, срок, задачи с результатом.
4. Навык как фильтр по задачам и проектам.
5. Собственные продукты со счётчиком звёзд GitHub.
6. Поле символов вместо фона, освещаемое курсором со шлейфом.
7. Расшифровка заголовков разделов при появлении в кадре.

### 1.3 User Stories

- Как рекрутер, я вижу за 10 секунд роль, цифры результата и способ связаться.
- Как нанимающий руководитель, я нажимаю на нужный мне навык и вижу только те
  задачи, где он применялся.
- Как владелец, я правлю резюме в `data/resume.js` и не трогаю вёрстку.

### 1.4 Критерии приёмки

- [x] Страница открывается без сборщика: с `python -m http.server` и двойным
      щелчком по `index.html`.
- [x] Клик по навыку подсвечивает совпадения и показывает их число; повторный
      клик и кнопка «Сбросить» снимают фильтр.
- [x] Фон — сетка символов; курсор освещает вокруг себя круг, свет гаснет с
      задержкой и тянется коротким шлейфом.
- [x] Заголовок раздела при появлении собирается из перебираемых знаков.
- [x] Декоративное свечение не выходит за края: горизонтальной прокрутки нет,
      под подвалом нет пустой полосы.
- [x] Поле рисуется позади содержимого: панели и текст его перекрывают.
- [x] На кадр перерисовывается только квадрат вокруг курсора, целиком — при
      изменении размера окна.
- [x] Свет не включается без мыши, мерцание не идёт при `prefers-reduced-motion`.
- [x] Без JavaScript видны имя, роль и контакты (`<noscript>`).
- [x] `python tools/build_single.py` собирает страницу в один файл.
- [x] На странице нет кнопок сохранения в PDF.

### 1.5 Non-goals

- Английская версия. Структура данных её допускает, содержание не готовилось.
- Светлая тема для экрана. Тёмная выбрана осознанно.
- Сохранение в PDF со страницы: печатная вёрстка выходила кривой, кнопки убраны
  по решению владельца от 2026-08-29. Правила `@media print` оставлены как
  подстраховка на случайный Ctrl+P, но функцией не считаются.
- CMS, форма обратной связи, аналитика посещений.
- Серверная часть любого вида.
- Автоматическая синхронизация с hh.ru.

### 1.6 Ограничения

- Хостинг — GitHub Pages, значит только статика.
- Публичная страница: почта доступна любому, кто откроет ссылку. Телефон на
  странице не публикуется — по решению владельца от 2026-08-29.
  `CONFIG.showSalary` убирает зарплатное ожидание.
- Шрифты берутся с Google Fonts; без сети подставляются системные из
  `font-family`.

---

## 2. Технологический стек

| Слой | Технология | Обоснование |
|------|-----------|-------------|
| Разметка | HTML5, одна страница | Резюме читается сверху вниз, маршрутизация не нужна |
| Оформление | CSS с переменными | Тема и печать решаются токенами без препроцессора |
| Логика | JavaScript ES5-совместимый, без модулей | Работает и по `file://`, и на Pages; нет сборки — нет расхождения исходника и результата |
| Поле символов | Canvas 2D | Несколько тысяч знаков в DOM — мусор в разметке и постоянные перерисовки макета |
| Данные | `data/resume.js` (`window.RESUME`) | Обычный тег `<script>` вместо `fetch` — иначе локальное открытие ломается о CORS |
| Скрипты | Python 3.12, стандартная библиотека | Уже стоит в окружении, зависимостей ставить не нужно |
| Публикация | GitHub Actions → GitHub Pages | Тот же путь, что у остальных репозиториев владельца |
| Проверка | Прогон в браузере через preview | Тестового фреймворка нет: логика целиком в отрисовке |

---

## 3. Структура папок

```
index.html              разметка-обвязка
data/resume.js          содержание резюме и CONFIG
data/stats.js           счётчики звёзд, генерируются при публикации
assets/css/style.css    оформление
assets/js/app.js        сборка страницы, фильтр, поле символов
scripts/fetch_stats.py  запрос звёзд к GitHub API
tools/build_single.py   сборка в один файл
```

## 4. Схема БД

Не применимо: состояние страницы — это `data/resume.js` в репозитории.
Браузерное хранилище не используется.

## 5. API-контракт

Не применимо: своего API нет. Единственный внешний вызов — `GET
https://api.github.com/repos/{owner}/{repo}` в `scripts/fetch_stats.py`, из
ответа берётся только `stargazers_count`. Недоступность API не ломает
публикацию: счётчики просто не показываются.

---

## 6. Definition of Done

- [x] Страница открывается без ошибок в консоли.
- [x] Все критерии приёмки из §1.4 проверены в браузере.
- [x] Репозиторий `akonyaev-ru/resume` публичный, Pages включены.

---

## 7. Журнал изменений и открытых вопросов

### Открытые вопросы

| # | Вопрос | Кому | Статус |
|---|--------|------|--------|
| 1 | Зарплатное ожидание на публичной странице | Владельцу | Оставлено сознательно |
| 2 | Ссылка на резюме в профильном README | Владельцу | OPEN |

### Журнал изменений контракта

| Дата | Версия | Что изменилось | Кто согласовал |
|------|--------|----------------|----------------|
| 2026-08-29 | 0.1 | Контракт заведён по факту первой версии страницы | — |
| 2026-08-29 | 0.2 | Репозиторий переименован в `resume` и открыт, Pages включены | Владелец |
| 2026-08-29 | 0.3 | Две рамки позиционирования свёрнуты в одну роль; тёмное оформление, новая типографика, след курсора | Владелец |
| 2026-08-29 | 0.4 | Телефон убран, почта открыта, логин телеграма убран; след курсора переехал на фон; метрики сведены к четырём по текущему месту; навыки перегруппированы | Владелец |
| 2026-08-29 | 0.5 | След за курсором заменён полем символов во весь фон; кнопки сохранения в PDF убраны | Владелец |
| 2026-08-29 | 0.6 | Свет за курсором гаснет с задержкой; заголовки разделов расшифровываются; свечение больше не вылезает за края страницы | Владелец |
