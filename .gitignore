dist/

# Папка сайта для выкладки: собирает tools/build_site.py в раннере из страниц,
# assets/ и data/ — в репозитории хранить нечего.
_site/

# Английская копия страницы: собирается tools/build_en.py в раннере перед
# выкладкой, как и dist/ — в репозитории хранить нечего.
en.html
__pycache__/
*.pyc
.DS_Store
Thumbs.db

# Рабочая память агента: внутренние заметки и приватные ссылки.
# Репозиторий публичный — им тут не место.
MEMORY.md

# Локальный запуск предпросмотра агентом: к содержимому сайта отношения не имеет.
.claude/

# Картинка со схемой, которую присылал владелец. На странице её нет — декор
# убран 2026-09-05, — но файл лежит в папке, и в репозиторий ему не надо.
Mask-group.png
